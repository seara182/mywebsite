.claude/
projects/*
!projects/cheapseats/
_notes/
private/master/CLAUDE.md
PWChange.bat
PWChange.ps1
projects/cheapseats/build-changelog.bat
Mika Jeske Design System.zip
Mika Jeske Design System

# build tooling — the compiled *.js files ARE committed (GitHub Pages serves them),
# but the toolchain that produces them is not
node_modules/
npm-debug.log*
.npm/

# browser automation / screenshots used while building the layout
.playwright-mcp/
playwright-report/
test-results/
*.local.png
screenshot*.png
seam*.png

# editors + OS junk
.vscode/
.idea/
*.code-workspace
.DS_Store
Thumbs.db
desktop.ini
*~
*.swp

# scratch + secrets
.env
.env.*
*.local
*.bak
*.tmp
*.log
.cache/

# internal briefs, not for the repo
DESIGN_GUIDE_anti-vibecode.md
RELEASE_BRIEF_v1.1.md
audit-report*.html
lighthouse*

# brag film render masters (with audio, ~150 MB). They live on iCloud and are
# reproducible with `node ci/build-brag.mjs`. The compositions themselves and
# the site copies in ci/assets/video/ stay tracked.
/brag-output/*.mp4
/brag-output/*.jpg
/brag-output/work-*/
/brag-vertical/*.mp4
/brag-vertical/*.jpg
